const nome = "Helena";
const sexo = "F";
const idade = 44;
const contribuição = 26;

const contribuiçãoMínima = 30;

const somaIC = idade + contribuição

if (contribuição >= contribuiçãoMínima) {
    if (somaIC >= 85) { console.log("Helena, você pode se aposentar!");
    } else {console.log("Helena, você não pode se aposentar!")
    }
    } else {console.log("Helena, você não pode se aposentar!")
    }